<resources>
    <style name="Theme.EStudenti" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/lavender</item> <!-- Consistent color -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/white</item> <!-- Ensure text color is visible -->
    </style>
</resources>
